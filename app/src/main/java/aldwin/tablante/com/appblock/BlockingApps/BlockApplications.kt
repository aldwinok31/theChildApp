package aldwin.tablante.com.appblock.BlockingApps

class BlockApplications (BlockApplications:ArrayList<String>) {
    var BlockApplications = BlockApplications

    constructor( ) : this(ArrayList())
}